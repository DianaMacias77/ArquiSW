//Giselle
describe('empty spec', () => {
  beforeEach(() => {
    cy.visit('http://localhost:3000')
  })

  it('contains at least one special char',() => {
    
  })

  it('does not contain at least one special char',() => {
    
  })

  it('from 8 to 10 characters',() => {

  })

  it('e')
})